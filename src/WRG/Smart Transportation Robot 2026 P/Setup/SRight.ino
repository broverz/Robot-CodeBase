void SRight() {
  resetYaw();
  delay(500);
  // mManual(45, 48, 3500);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);
  gyroFL(76);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(285, 55);
  gyroFR(76);
  gyroFF(100, 55);
  grabBk();
  gyroFL(82);
  gyroFF(200, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);
  gyroFR(76);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(150, 55);
  gyroFL(76);

  lineTrackingPID();
  resetYaw();
  delay(100);
  gyroFF(320, 55);
  putBk();
  gyroFR(160);
  gyroFF(320, 55);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);
  gyroFL(76);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);
  gyroFR(76);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);
  gyroFR(76);

  lineTrackingPID();
  resetYaw();
  delay(50);
  gyroFF(160, 55);

  // lineTrackingPID();
  // resetYaw();
  // delay(50);
  // gyroFF(150, 55);
  // gyroFR(76);

  // lineTrackingPID();
  // resetYaw();
  // delay(50);
  // gyroFF(150, 55);
  // putBk();

  // gyroFR(160);

  // lineTrackingPID();
  // resetYaw();
  // delay(50);
  // gyroFF(150, 55);
  // gyroFR(76);

  // lineTrackingPID();
  // resetYaw();
  // delay(50);
  // gyroFF(150, 55);
  // gyroFL(76);

  // gyroFF(180, 55);

  // grabBk();

  // gyroFR(80);
  // gyroFF(50,55);

  // lineTrackingPID();
  // resetYaw();
  // delay(50);
  // gyroFF(150, 55);

  // gyroFF(275, 55);

  // putBk();
}